package com.erensaridag.careermatch.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Brand Colors - Vibrant Orange to Purple Gradient (ORIGINAL)
val PrimaryGradientStart = Color(0xFF1F556B) // Vibrant Orange
val PrimaryGradientEnd = Color(0xFF6B9836) // Rich Purple
val AccentColor = Color(0xFF2F93BC) // Dark Purple

// Secondary Colors
val SuccessColor = Color(0xFF10B981) // Emerald-500 (Kept for semantics)
val WarningColor = Color(0xFFF59E0B) // Amber-500 (Kept for semantics)
val ErrorColor = Color(0xFFEF4444) // Red-500 (Kept for semantics)
val InfoColor = Color(0xFF3B82F6) // Blue-500 (Kept for semantics)

// Neutral Colors
val CardBackground = Color(0xFFF7FAFC) // Very light gray
val DarkText = Color(0xFF0E2A36) // Dark Gray from palette
val LightText = Color(0xFF5A7A86) // Beige from palette
val DividerColor = Color(0xFFD7E3EA) // Gray-200
val BackgroundLight = Color(0xFFF4F8FA) // Gray-50

// TextField Backgrounds - LIGHTER for better readability
val TextFieldBackground = Color(0xFFEEF3F6) // Very light gray
val TextFieldBackgroundFocused = Color(0xFFFFFFFF) // White when focused
val CardBackgroundLight = Color(0xFFFFFFFF) // White for lighter cards

// Surface colors for better layering
val SurfaceLight = Color(0xFFFFFFFF) // White for elevated surfaces
val SurfaceDark = Color(0xFFF0F6F8) // Very light gray for base surfaces

// Dark Theme Background
val BackgroundDark = Color(0xFF0D2B38) // Deep Purple from palette

// Navy Blue Accents (kept for compatibility but not primary)
val NavyDark = Color(0xFF0B2B3A) // Very dark navy
val NavyMedium = Color(0xFF1F556B) // Medium navy
val NavyLight = Color(0xFF2F93BC) // Lighter navy blue

// Material 3 Theme Colors (Light)
val Purple80 = PrimaryGradientStart
val PurpleGrey80 = AccentColor
val Pink80 = PrimaryGradientEnd

// Material 3 Theme Colors (Dark)
val Purple40 = PrimaryGradientEnd
val PurpleGrey40 = AccentColor
val Pink40 = PrimaryGradientStart